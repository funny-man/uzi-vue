import { expect } from 'chai';
// import { mount } from '@vue/test-utils';
// import zButton from '@/components/button/button.vue';

describe('Button.vue', () => {
  // 现在挂载组件，你便得到了这个包裹器
  // const wrapper = mount(zButton);

  it('渲染正确的标记', () => {
    expect(1).to.eq(1);
  });
  // it('button禁用状态测试', () => {
  //   const disabled = true;
  //   // console.log(wrapper.vm);
  //   expect(wrapper.props).to.include(disabled);
  // });
});
